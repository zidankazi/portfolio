'use client';

import { motion, useReducedMotion } from 'framer-motion';
import type { Project } from '@/data/projects';

export const ProjectCard = ({ project }: { project: Project }) => {
  const prefersReducedMotion = useReducedMotion();

  return (
    <motion.article
      whileHover={prefersReducedMotion ? undefined : { y: -4 }}
      transition={{ duration: 0.2 }}
      className="group flex flex-col justify-between rounded-xl border border-anthropic-border bg-white p-6 shadow-sm transition-shadow hover:shadow-md"
    >
      <div>
        <div className="flex items-start justify-between gap-4 mb-4">
          <h3 className="text-lg font-medium text-anthropic-text">{project.title}</h3>

          <div className="flex gap-3 text-xs tracking-wider">
            {project.links.map((link) => (
              <a
                key={link.label}
                href={link.href}
                className="text-anthropic-text/50 hover:text-anthropic-text transition-colors underline underline-offset-4 decoration-anthropic-text/20 hover:decoration-anthropic-text/50"
                target="_blank"
                rel="noreferrer"
              >
                {link.label}
              </a>
            ))}
          </div>
        </div>

        <p className="text-sm text-anthropic-text/70 leading-relaxed mb-6">
          {project.description}
        </p>

        <div className="flex flex-wrap gap-2 mb-6">
          {project.tags.map((tag) => (
            <span
              key={tag}
              className="rounded bg-anthropic-bg px-2 py-1 text-[11px] tracking-wider text-anthropic-text/60 border border-anthropic-border/50"
            >
              {tag}
            </span>
          ))}
        </div>
      </div>

      <ul className="space-y-3 text-sm text-anthropic-text/80">
        {project.bullets.map((bullet) => (
          <li key={bullet} className="flex items-start gap-3">
            <span className="mt-2 block h-1 w-1 shrink-0 rounded-full bg-anthropic-text/30" />
            <span className="leading-relaxed">{bullet}</span>
          </li>
        ))}
      </ul>
    </motion.article>
  );
};
